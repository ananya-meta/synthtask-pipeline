[project]
name = "synthtask-pipeline"
version = "0.1.0"
description = "Synthetic task pipeline: research discovery -> scaffold -> validation feedback"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
synthtask = "ideation.cli:main"
ideation = "ideation.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ideation"]
