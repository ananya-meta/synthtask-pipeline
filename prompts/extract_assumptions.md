<!-- TODO: YOU write this (optional; the flow works without it).

     Purpose: given a paper, list the load-bearing assumptions whose violation would
     create a hard engineering problem. Output goes into the seed's ASSUMPTIONS.md,
     which is then interpolated into generate_ideas.md as {{ASSUMPTIONS}}.

     Hassan's finding: starting from a paper but changing a key assumption produced
     interesting tasks. Reimplementing the paper produced rejected ones.
-->
