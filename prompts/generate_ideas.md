<!-- TODO: YOU write this. Claude must not — see README.md in this directory.

     The harness refuses to run while this file starts with "<!-- TODO", so it will
     fail loudly rather than generate against a placeholder.

     Draft it from a manual run: ask TBH for ideas on one paper, reject the bad ones,
     and turn each rejection into a rule here.

     Available substitutions:
       {{N_IDEAS}} {{SEED_TITLE}} {{SEED_SLUG}} {{ASSUMPTIONS}} {{OUTPUT_PATH}}

     Structure that the track's findings suggest works:
       1. Role + what a good benchmark task is
       2. The seed: title, and the assumptions block
       3. Hard constraints  (binary reward; real datasets only; no links in the task;
          never reveal evaluation mechanics; break an assumption rather than
          reimplement the paper)
       4. Explicit anti-patterns to avoid  (mirror config/antipatterns.json)
       5. Diversity requirement across the {{N_IDEAS}} ideas
       6. Output contract: write {{OUTPUT_PATH}} in the schema from README.md
-->
