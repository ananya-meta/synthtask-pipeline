<!-- TODO: YOU write this. Claude must not — see README.md in this directory.

     Five dimensions the harness persists (ideation/judge.py DIMENSIONS):
       novelty          not recallable from training data
       feasibility      can actually be scaffolded into a task env
       difficulty       a frontier model would not one-shot it
       interestingness  genuinely worth solving, not busywork
       verifiability    a clean binary oracle exists

     Define each on an explicit numeric scale with anchored descriptions, the way the
     Marketplace top-content dimensions are defined. Anchors are what make the judge
     reproducible across rubric versions.

     The rubric version is the hash of this file, so any edit starts a fresh, separately
     tracked scoring generation — `ideation report` shows agreement per version.
-->
